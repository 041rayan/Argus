/**
 * Engine package: pipeline, scanner, API clients, KEV analysis, crypto.
 * No javafx.* imports allowed here — enforced by the boundary test.
 * Domain records live in core/model, DTOs in core/api/dto.
 */
package com.argus.core;
